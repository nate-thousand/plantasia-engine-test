/** ASCII raster dimensions — ~10× cell count vs original 15×11 canvas. */
export const GRID_WIDTH = 47;
export const GRID_HEIGHT = 33;
export const GRID_CENTER_X = Math.floor(GRID_WIDTH / 2);
export const GRID_CENTER_Y = Math.floor(GRID_HEIGHT / 2);

# Styles

Global stylesheets.

| File | Purpose |
|------|---------|
| `main.scss` | Foundation boot — tokens, Bootstrap core (reboot), theme |
| `_bootstrap-core.scss` | Bootstrap 5.0.2 config + reboot only |
| `bootstrap-full.scss` | Full Bootstrap component bundle — import when building UI |
| `_bootstrap-theme.scss` | CSS variable mapping for Bootstrap runtime |

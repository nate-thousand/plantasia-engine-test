# Preset Loading

## Purpose

Load and manage presets provided by `plantasia-sound-engine`.

## Planned Responsibilities

- Resolve bundled and custom preset sources
- Apply preset selection to the active engine instance
- Cache loaded preset metadata for UI and ASCII identity

## Boundaries

- Preset schema and bundled JSON remain in the sound engine repository
- This layer only coordinates loading and application state

## Status

Placeholder. Implementation pending.
